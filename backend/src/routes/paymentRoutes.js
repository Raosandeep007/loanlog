/**
 * Payment Routes
 */

const express = require('express');
const router = express.Router();

// Placeholder routes
router.get('/', (req, res) => {
  res.json({ success: true, message: 'Payment routes - implementation pending' });
});

module.exports = router;
